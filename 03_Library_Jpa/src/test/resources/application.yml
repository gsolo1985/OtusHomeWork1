spring:
  datasource:
    url: jdbc:h2:mem:testdb
    data: test-data.sql
    initialization-mode: always
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
  shell:
    interactive:
      enabled: false